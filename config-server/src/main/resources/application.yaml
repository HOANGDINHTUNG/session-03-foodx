server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/HOANGDINHTUNG/session-03-foodx
          clone-on-start: true
          default-label: master